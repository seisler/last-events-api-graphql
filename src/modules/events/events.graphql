type Query {
    getEvents: [Event]
    event(id: ID!): Event
}

type Mutation {
    createEvent(createEventInput: CreateEventInput): Event
}

type Subscription {
    eventCreated: Event
}

type Event {
    id: Int
    name: String
    description: String
}

input CreateEventInput {
    name: String
    description: String

}

